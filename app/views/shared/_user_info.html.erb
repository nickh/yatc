<div class='user_info'>
  <a href='<%= user_path(current_user) %>'>
    <%= gravatar_for current_user, :size => 30 %>
    <span class='user_name'>
      <%= current_user.name %>
    </span>
    <span class='microposts'>
      <% if current_user.microposts.empty? %>
        No microposts
      <% else %>
        <%= pluralize(current_user.microposts.count, 'micropost') %>
      <% end %>
    </span>
  </a>
</div>
