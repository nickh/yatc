<h1>Edit user</h1>

<%= form_for(@user) do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  <%= render 'form', :f => f, :submit_tag => 'Update' %>
<% end %>

<div>
  <%= gravatar_for @user %>
  <a href='http://gravatar.com/emails'>change</a>
</div>
